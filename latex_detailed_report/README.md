# SR3 项目二与项目三超详细 LaTeX 文档

主文件：

```text
main.tex
```

章节位于 `chapters/`。文档会从上级目录的 `project2/`、`project3/`
直接读取 MATLAB 源码作为附录。

推荐使用 XeLaTeX 编译：

```powershell
xelatex main.tex
xelatex main.tex
```

最终提交前需要自行补充真实运行截图、曲线、测试输出，以及封面个人信息。
